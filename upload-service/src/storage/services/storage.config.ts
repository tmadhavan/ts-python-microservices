// The name (NestJS: 'token') of the storage service provider which the storage module provides
export const STORAGE_SERVICE = 'STORAGE_SERVICE';

export const enum STORAGE_TYPE {
  AWS = 'AWS',
  AZURE = 'AZURE',
}
